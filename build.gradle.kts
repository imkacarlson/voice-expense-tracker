plugins {
    // Version resolution handled via Gradle plugin portal in real builds
    // Keeping minimal root build file for structure
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

